# Survey Table Project — 도메인 언어

설문 빌더·응답·운영 플랫폼의 도메인 용어집. 아키텍처 리뷰와 설계 대화에서 이 용어를 정본으로 쓴다.

## Language

### 테이블 질문 평가

**테이블 셀 의미론 (table-cell-semantics)**:
테이블 셀 응답값의 해석·판정 규칙 전체 — optionId 언랩, optionId→옵션 value 해석, input 값 정본화(`String(v).trim()`), 응답됨 판정, 기대값 매칭, isHidden 정책. `src/utils/table-cell-semantics.ts` 한 곳이 소유한다.
_Avoid_: 셀 값 추출 로직, 셀 타입 switch

**검증 규칙 (table validation rule)**:
테이블 응답에 대한 분기 규칙. 5종 수량자(exclusive-check / any-of / all-of / none-of / required-combination)를 가지며, 이 수량자 어휘는 검증 규칙 interface 소유다 — 셀 의미론으로 내리지 않는다. 차단형 검증과 별개 — 이쪽은 분기(end/goto)만 하고 진행을 차단하지 않는다.
_Avoid_: 테이블 밸리데이션, 검증 조건

**차단형 검증 (blocking validation)**:
숫자 입력에 대한 진행 차단형 검증 계열 — min 미달, 합계 제약, 필수 셀. "다음"/제출 시점에 평가되어 위반 시 인라인 에러를 표시하고 진행을 막는다. 검증 규칙(분기 전용)과 달리 응답 흐름을 바꾸지 않으며, 클라이언트 전용이다(서버 재검증 없음 — 비목표로 확정).
_Avoid_: 검증 규칙(분기 어휘와 충돌), 제출 검증

**합계 제약 (sum constraint)**:
차단형 검증의 규칙 엔티티 — 임의 선택한 숫자 input 셀 집합의 합이 목표값과 =/≤/≥ 관계를 만족해야 한다. 대상 셀이 전부 빈 값이면 평가를 스킵한다(미접촉 허용 — 미응답 차단은 필수 여부 소관).
_Avoid_: 합계 규칙, 100% 검증

**수식 (cell formula)**:
계산 셀과 수식 검증이 공유하는 중첩 그룹 수식 트리. 그룹 = 연산자 1개 + 항 N개(연산자를 섞으려면 그룹을 중첩 — 우선순위 모호성이 구조적으로 불가능). 항은 셀 참조·질문 참조·LUT·상수·SUM/AVG 집계·하위 그룹. 표시 조건은 평가하지 않는다(응답 인용과 동일 결정).
_Avoid_: 계산식 문자열, 엑셀 수식

**계산 셀 (calc cell)**:
표 질문의 읽기 전용 셀 — 수식을 실시간 파생 표시하고, 저장 시점에 응답으로 주입된다(키스트로크 저장 없음). 표시·저장·검증 비교가 같은 반올림 하나를 공유한다("화면에 보인 값이 곧 응답"). 운영 콘솔 수정 화면에서도 읽기 전용 — 수식 결과와 다른 저장값이라는 상태는 존재하지 않는다.
_Avoid_: 자동합계 셀, 계산 필드, 파생 셀

**셀 게이팅 (cell gating)**:
같은 행의 컨트롤러 셀 값에 따라 대상 input 셀의 입력 가능 여부를 제어하는 활성 조건(`enabledWhen`). 컨트롤러 미응답은 미충족 = 비활성이며, 비활성화 순간 값은 키 삭제로 지워지고 저장 경계에서도 strip 된다. 비활성 셀은 모든 차단형 검증에서 제외된다. 게이팅 셀의 필수는 "활성일 때 필수" 하나로 수렴한다. 표시 조건은 평가하지 않는다(수식·응답 인용과 동일 결정). prefill 셀과는 양립 불가 — 설정 자체를 금지한다.
_Avoid_: 셀 분기(분기 어휘와 충돌), 조건부 표시(표시조건과 충돌 — 게이팅은 보이되 잠그는 것)

**수식 검증 (formula validation)**:
차단형 검증의 규칙 — 숫자 input 셀의 입력값을 수식 계산값과 비교해 불일치 시 진행을 차단한다. 빈 입력은 스킵(입력 강제는 필수 여부 소관 — 합계 제약과 같은 계열). 역계산이 아니라 정계산 결과와의 비교다. 수식이 `null`(순환·LUT 미해결)이면 통과 — 환경 문제로 응답자를 막지 않는다.
_Avoid_: 역계산, 합계 검증(합계 제약과 충돌)

**필수 마스터 전파 (required master propagation)**:
질문 레벨 "필수 질문" 토글을 조작하는 순간 표의 인터랙티브 셀 필수와 보기 옵션 그룹 필수를 일괄 재설정하는 편집기 동작. 상속·파생이 아니라 조작 시점 복사다 — 발행된 설문의 저장값에는 소급하지 않고, 이후 셀·그룹 개별 수정이 가능하다. ON/OFF 대칭이며, 게이팅 셀은 "활성일 때 필수"로 켜고, 그룹의 명시 오버라이드는 제거해 상속으로 복귀시키고, 셀·그룹별 안내 문구는 보존한다.
_Avoid_: 필수 상속(그룹의 파생 규칙과 혼동 — 이쪽은 복사), 필수 동기화

**표시조건 (displayCondition)**:
질문·그룹·행·열·동적 그룹의 노출 여부를 결정하는 조건 그룹(AND/OR/NOT + 조건 목록). 테이블 검사 시 checkType(any/all/none) 어휘를 쓰며 검증 규칙의 5종 수량자와 별개 interface다.
_Avoid_: 분기 조건(분기는 검증 규칙의 action 쪽 어휘)

**응답됨 (answered)**:
셀에 유효한 응답값이 존재하는 상태. 두 변종이 실재한다 — 인터랙티브 타입 게이트 판정(매칭의 기본값)과 타입 불문 값 존재 판정(exclusive-check 전수 스캔). 둘은 다른 판정이며 함수 이름으로 구분한다.
_Avoid_: 체크됨(checkbox 한정 어감), 입력됨

**행 완료 판정 (row completion)**:
테이블 행의 모든 answerable 셀이 응답됐는지의 판정 (`table-row-completion.ts`). isHidden 셀과 radioGroup 버킷을 제외/그룹 단위 처리한다. 셀 의미론과 같은 개념을 공유하지만 빈 배열·공백 처리의 의미가 달라 별도 module로 둔다.

**branch-eval**:
분기 평가의 leaf 의존 — `BranchEvalCtx`, `emptyBranchEvalCtx`, `evaluateNumericComparisonV2`. 셀 의미론과 branch-logic이 모두 의존하는 순환 없는 최하층(`src/utils/branch-eval.ts`).

### 동적 행과 응답 쓰기

**동적 행 파이프라인 (dynamic-row pipeline)**:
동적 행 기능의 단일 진입점 — 선택 상태 → 가시 행 필터링(동적 그룹 제외 + rowspan 재계산) → 셀렉터 배치·grid 좌표 → 행 완료 맵까지의 호출 순서와 배선. `src/hooks/use-dynamic-rows.ts`(facade)가 소유하고, 내부 두 훅(use-dynamic-row-state, use-dynamic-row-layout)은 implementation이다. displayCondition 평가는 호출자 소유로 결과만 주입받는다.
_Avoid_: 레이아웃 훅 무리, 동적 행 로직

**질문 응답 쓰기 채널 (question response writer)**:
"최신 응답 읽기 → 패치 병합 → 커밋" 의식의 단일 거처(`src/hooks/use-question-response-writer.ts`). 테스트 모드(test-response-store)와 실응답 모드(value/onChange)라는 두 adapter가 이 seam을 만족한다. 새 응답 쓰기 지점은 isTestMode 분기를 만들지 말고 이 채널을 경유한다.
_Avoid_: 모드 분기, isTestMode 스위치

**SurveyDiffPayload 조립 (diff-payload)**:
changeset snapshot + 현재 설문 상태 → 저장 payload 변환 규칙(`src/lib/survey-builder/diff-payload.ts`, 순수 함수). dirtyIds = added∪updated 필터, 메타데이터 조건부 필드, reordered 전체 id 순서를 소유한다. use-survey-sync는 저장 오케스트레이션만 담당한다.

### 응답 페이지 표시

**모바일 테이블 표시 모드 (mobile table display mode)**:
질문별 모바일 테이블 표현 정책. 자동 카드, 선택 행 원본 보기, 행별 원본 문항 보기, 전체 원본 표 중 정확히 하나이며 여러 독립 토글의 조합이 아니다.
_Avoid_: 모바일 표 옵션들, 원본 표 boolean 조합

**반복 헤더 범위 (repeated header range)**:
선택 행 원본 보기와 행별 원본 문항 보기의 각 원본 표 조각 상단에 반복할 정식 헤더와 작성 본문 행의 연속 범위. 0은 정식 헤더 전체, 1 이상은 현재 tableRowsData 작성 위치를 뜻하며, 빈 값은 반복 없음이다. 범위는 행 ID가 아니라 현재 작성 순서를 따르되, 실제 반복 블록에는 표시조건·동적 선택을 통과한 현재 가시 행만 포함한다.
_Avoid_: 반복 행 ID 목록, 모바일 헤더 개수

**반복 본문 행 (repeated body row)**:
반복 헤더 범위에 포함되어 모바일 목차·진행률에서는 제외되고 각 선택 상세에서 표시 전용 원본 행으로 재사용되는 작성 본문 행. 입력 셀이 있어도 응답값을 주입하지 않은 비활성 PreviewCell로 렌더되며, 이 비활성 처리는 해당 모바일 상세에만 적용된다.
_Avoid_: 응답 행 복제, 고정 행

**선택 행 원본 보기 (selected-row original view)**:
모바일 테이블에서 항목은 드릴다운으로 탐색하고, 선택한 행의 나머지 셀은 원래 열 관계를 유지해 보여주는 표시 방식.
_Avoid_: 10점 척도 모드, 모바일 원본 표

**원본 행 상세 설정 (original row detail settings)**:
선택 행 원본 보기와 행별 원본 문항 보기가 공유하는 원본 표 조각의 표현 설정. 상세 제외 선행 열 수와 반복 헤더 범위를 함께 묶으며, 표시 모드를 전환해도 설정값은 유지된다.
_Avoid_: 드릴다운 전용 설정, 행별 문항 전용 설정

**행별 원본 문항 보기 (row-wise original question view)**:
모바일 테이블에서 반복 헤더 범위 밖의 각 응답 행을 독립 문항처럼 하나의 연속 설문 시트에 세로로 나열하고, 각 행의 셀은 원래 열 관계를 유지해 보여주는 표시 방식. table·radio·checkbox 모두 같은 조건·동적 선택 가시성 규칙을 사용하며, 이를 통과하고 현재 응답 가능한 셀이 하나 이상인 행만 문항이 되어 선택 시점과 무관하게 원본 작성 순서를 유지한다. 행별 가로 스크롤 위치는 서로 동기화하지 않고, 화면 표현만 행 단위로 나눌 뿐 저장·검증·분기·내보내기에서는 하나의 테이블 질문과 기존 셀 응답 구조를 유지한다.
_Avoid_: 행별 질문 분할, 다중 질문 변환

**행별 원본 문항 제목 (row-wise original question title)**:
행별 원본 문항에서 원본 표 조각 위에 표시하는 행의 대표 라벨. 상세 제외 선행 열 중 가장 오른쪽의 보이는 텍스트·이미지·비디오 셀을 우선하고, 없으면 행 라벨, choice 옵션 라벨 순으로 대체한다.
_Avoid_: 첫 번째 열 라벨, 행 번호

**행별 원본 문항 섹션 (row-wise original question section)**:
연속 설문 시트에서 여러 행별 원본 문항을 묶는 상위 구조 라벨. 가시 문항이 하나뿐이고 행 제목과 같은 라벨이면 중복 표시하지 않으며, 여러 문항을 묶거나 행 제목과 다른 상위 맥락을 제공할 때만 표시한다. 하위 그룹이 있으면 해당 그룹의 첫 가시 문항 앞에 보조 구분으로 한 번만 표시한다.
_Avoid_: 입력 없는 빈 문항, 중복 행 제목

**행별 병합 셀 구체화 (row-wise merged cell materialization)**:
여러 작성 행에 걸친 rowspan 셀을 각 행별 원본 문항에서 독립적으로 읽히는 한 행 셀로 펼쳐 보여주는 규칙. 구체화된 입력 셀은 복제 응답을 만들지 않고 원본 셀 정체성과 선택 그룹 의미를 유지한다.
_Avoid_: 병합 셀 복제, 새 응답 셀

**셀 표시 인스턴스 (cell display instance)**:
행별 병합 셀 구체화로 같은 논리 셀이 여러 행에 보일 때 각 DOM 표시를 구별하는 정체성. 입력 id·label 연결·포커스·오류 위치 이동에는 행과 원본 셀을 조합한 고유 표시 인스턴스 ID를 사용하지만, 응답 저장과 choice 그룹 의미에는 기존 논리 셀 ID를 사용한다.
_Avoid_: 복제 셀 ID, 새 응답 셀 ID

**행별 원본 열 좌표 (row-wise original column coordinate)**:
행별 원본 문항에서 각 셀이 차지하는 원본 작성 열의 위치. 비대칭 행에서 중간 셀이 없더라도 뒤쪽 셀을 앞으로 당기지 않고 빈 자리와 헤더 대응을 유지한다.
_Avoid_: 행별 셀 압축, 입력 순서 재배치

**행별 원본 문항 오류 상태 (row-wise original question error state)**:
사용자가 검증을 시도한 뒤 오류가 확인된 행 문항에만 적용하는 피드백 상태. 평소의 행은 완료 여부를 표시하지 않는 중립 상태이며, 오류 행 제목과 실제 오류 셀을 식별하고 첫 오류의 셀 표시 인스턴스로 스크롤·포커스한다.
_Avoid_: 행별 완료 상태, 행 단위 진행률

**상세 제외 선행 열 수 (omitted leading column count)**:
선택 행 원본 보기와 행별 원본 문항 보기의 원본 표 조각에서 반복하지 않을 원본 테이블 왼쪽 선행 열 개수. 현재 보이는 열이 아니라 작성된 열 순서를 기준으로 하며 기본값은 1이다.
_Avoid_: 첫 번째 가시 열, 숨김 열 수

**모바일 셀 표시 설정 (mobile cell display)**:
모바일 카드·선택 행 원본 보기에서 셀 콘텐츠나 입력 라벨을 어떻게 노출할지 정하는 셀별 정책. `hidden`은 표시 셀의 콘텐츠를 숨기지만 인터랙티브 셀의 응답 컨트롤 자체를 제거하지 않는다.
_Avoid_: 구조적 숨김, 입력 셀 제거

**옵션 라벨 줄바꿈 (option label line break)**:
radio/checkbox 옵션 라벨과 표 choice 셀 텍스트에 저장된 개행 문자의 취급 규칙 — 표시 전용이다. 응답 화면(데스크탑·모바일·미리보기·테스트·운영 콘솔 수정)에서는 입력한 줄바꿈을 보존해 렌더하고, 통계 납품물(SPSS value label, 엑셀 export 라벨)에서는 공백으로 정규화해 단일 행 라벨을 유지한다. 응답값 매칭·분기에는 관여하지 않는다(매칭은 value/cell.id 기준).
_Avoid_: 라벨 개행 지원(범위 불명), 멀티라인 옵션

**응답 페이지 헤더 (response page header)**:
공개 설문 응답 화면 상단에서 설문 제목, 로고, 통계법 문구 같은 설문지 머리말 요소를 표현하는 영역. 응답 진행률이나 질문 입력 영역과 별개의 설문 정체성 표현이다.
_Avoid_: 상단 장식, 레거시 헤더

**헤더 로고 (response header logo)**:
응답 페이지 헤더에 표시되는 기관·승인 로고 이미지. 질문 본문 이미지가 아니라 설문지 머리말의 정체성 표시 자산이며, 설문 이미지 자산으로 취급한다.
_Avoid_: 장식 이미지, 질문 이미지

**기본형 헤더 (plain response header)**:
로고와 통계법 문구 없이 제목 중심으로 표시되는 응답 페이지 헤더 프리셋. 기존 화면의 픽셀 보존 모드가 아니라 로고 없는 설문의 기본 표현이다.
_Avoid_: 레거시 호환 모드

### 질문 유형과 정규화

**질문 유형 레지스트리 (question type registry)**:
9개 질문 유형 리터럴의 런타임 SSOT(`src/types/question-types.ts`) — `QUESTION_TYPES`와 그룹 상수 5종(내장 테이블 / 모바일 테이블 표시 / choice 그룹 / 옵션 목록 / 코드드 choice) + 멤버십 가드. 유형 멤버십 분기는 사설 Set·배열을 만들지 않고 여기를 경유한다. TS QuestionType과의 양방향 동치는 컴파일 프로브로 강제된다.
_Avoid_: 인라인 type 배열 비교, needsOptions류 사설 분기 함수

**질문 variant (QuestionVariant)**:
유형별 필드 소유를 박제한 판별 유니언(`src/lib/question/variants.ts`). 필드 타입의 단일 출처는 flat Question이고 variant는 Pick 합성이다 — flat으로의 단방향 할당 호환(`toFlatQuestion`)이 유지 축. 내장 테이블 capability는 table 전용이 아니라 radio·checkbox·ranking·table 4유형 공유다. TS variant ↔ zod 스키마의 키셋 동치는 드리프트 게이트가 지킨다. 분류 가드(`is*Question`, guards.ts)는 전부 "유형 멤버십"이다 — choiceGroups 데이터 실재로 분기하는 isGroupedChoiceQuestion(grouped 응답 shape 어휘)과 별개 개념이며, 후자를 멤버십 가드로 치환하면 required 영구 미충족/무력화 사고가 난다.
_Avoid_: 테이블 필드 = table 유형 전용이라는 가정, isChoiceGroupCapableQuestion(멤버십)과 isGroupedChoiceQuestion(데이터 실재)의 혼용

**질문 정규화 경계 (question normalize boundary)**:
스냅샷·export 등 신뢰 불가 직렬화 데이터가 `Question[]`로 들어오는 읽기 경계의 단일 거처(`src/lib/question/normalize.ts`). preserve(판별자만 검증, 무변형 passthrough + 관측)와 strict(zod parse + cross-type 오염 키 소거) 2모드. 경계 캐스트는 이 module 안 한 곳에만 존재한다 — 새 역직렬화 지점에서 `as unknown as Question[]`을 만들지 않는다.
_Avoid_: 역직렬화 단언, 호출처별 자가 검증

### 설문 수명주기

**응답 버전 이관 (response version migration)**:
구버전에 고정된 미완료 응답(진행중·이탈)을 재개 시점에 현재 발행 버전으로 재고정하고, 기존 답변을 구조 생존 판정으로 걸러 얹는 처리. 재개(재방문 복원, 구버전 탭의 첫 응답 생성 포함) 시점에만 일어나며, 이미 응답 중인 탭은 시작한 버전으로 끝까지 진행·제출한다. 완료·판정 종료(screened_out/quotaful_out/bad) 응답은 대상이 아니다. 이관해도 원래 버전은 이관 출처 기록으로 응답에 남고, 이관 출처가 참조하는 버전 스냅샷은 프루닝에서 보호된다. (ADR-0014)
_Avoid_: 응답 초기화, 버전 마이그레이션(스키마 마이그레이션과 혼동), 구버전 고수

**구조 생존 판정 (structural survival)**:
응답 버전 이관에서 기존 답변의 유지/폐기를 가르는 규칙 — 질문·셀 타입(응답값의 모양)이 같고 참조하는 옵션·셀이 신버전에 실존하면 유지한다. 타입이 바뀌면 그 질문의 답만 폐기하고, 삭제된 옵션·셀을 가리키는 고아 값은 답 전체가 아니라 값 단위로만 제거한다. 단답형 inputType 변경은 모양이 살아있으므로 유지다. 문구·라벨의 의미 변화는 판정하지 않는다 — 완료 응답도 똑같이 노출되는 문제라, 판단은 배포하는 관리자 몫이다. 답이 폐기·제거되면 재개 위치는 영향받은 가장 앞 페이지로 되돌린다.
_Avoid_: 의미 비교 검증, 답변 전체 초기화, numeric/string 폐기(타입은 응답값 모양 층위)

**설문 복제 (survey duplication)**:
설문의 설계(설정·질문 그룹·질문·LUT 사본)만 물려받는 새 설문 생성. 운영 데이터(응답·컨택·메일·발행 버전)와 실사 세팅(쿼터·컨택 컬럼 스킴·결과코드·진척률 컬럼)은 물려받지 않는다. 복제본은 초안(draft)으로 시작하고 발행 이력이 비어 있어 첫 발행이 v1이다 — 원본의 발행 횟수와 무관하다. 응답 링크(slug·private token)는 새로 발번되고 선택지 옵션 코드도 재발번된다.
_Avoid_: 설문 복사(응답까지 복사되는 어감), 스냅샷 복제

**로컬 초안 (local draft)**:
미제출 응답의 브라우저 보관 사본 — 서버 쓰기가 전부 실패하는 구간(크래시·네트워크 단절)에서 답변을 지키는 보조 계층. 저장된 세션과 같은 스코프(설문+초대토큰)·수명을 가지며, 재개 시 서버 초안과 최신순으로 우선을 판정한다. 최신순의 척도는 벽시계가 아니라 저장 순번(seq)이고, 동률이면 항상 서버가 이기며, 병합은 하지 않는다(낡은 쪽 소량 유실 감수). 응답 PII는 담지 않는다 — PII 복원은 우선 판정과 무관하게 항상 서버 몫. 제출 완료·관리자 수정 감지·보존 기한(7일) 경과 시 폐기된다. (2026-08-12 설계 확정, 구현 보류)
_Avoid_: 로컬 캐시(임의 폐기 가능한 어감), localStorage 백업(구현 층위), 오프라인 모드(별개 기능 어감)

### 문항 수요조사

**수요조사 (question demand survey)**:
지난 회차의 종이 조사표를 놓고 "이 문항을 다음 회차에도 쓸 것인가"를 판단받는 설문 형식. 응답자는 자문위원·발주처 담당자 등 소수이고 데스크톱 전용이다. 이 형식을 위해 신설된 것은 조사표·영역 앵커·분할 레이아웃 셋뿐이며, 질문 유형·조건부 표시·쿼터·컨택·초대·메일·export 는 전부 기존 것을 그대로 쓴다. 판단 항목과 문항 의견도 전용 질문 유형이 아니라 평범한 문항 둘이다.
_Avoid_: 문항 평가 설문(평가는 응답자 능력 측정 어감), 조사표 리뷰(대상이 문서라는 어감)

**판단 항목 (judgement item)**:
수요조사에서 조사표의 문항 하나에 대응하는 "필요함 / 필요하지 않음" 단일선택. 판정은 이 둘 중 하나이며 제3의 값이 없다 — 판정 없이 의견만 내는 것은 허용하지 않는다(2026-09-03 결정). 필요율의 분모는 이 두 값의 합이다. 이전에는 '의견'이 세 번째 선택지였고 분모에만 들어가 필요율을 끌어내렸다.
_Avoid_: 평가 항목(응답자 능력 측정 어감), 체크 항목(체크박스와 혼동)

**문항 의견 (item opinion)**:
판단 항목에 **딸리는** 자유 서술. 어느 판정을 골랐든 붙일 수 있고 비어 있어도 정상이다 — 판정과 직교한다. 응답 화면에서는 판단 항목과 한 줄로 보이지만 모델에서는 판단 항목 바로 뒤에 오는 별개의 장문형 문항이며, 문항코드 규약(부모 코드 + `_T`)으로 짝을 알아본다. 집계에서는 판정에 영향을 주지 않고 의견 수와 본문으로만 나타난다. (ADR 0022)
_Avoid_: 기타 상세기재(선택지에 종속하는 다른 것), 코멘트(운영 메모와 혼동), 의견 선택지(더 이상 선택지가 아니다)

**기타 상세기재 (other-option detail)**:
선택지에 **종속하는** 서술. "기타(적을 것: ___)"처럼 그 선택지를 골랐을 때만 의미가 있고, 골라 놓고 비우면 미응답이다. 문항 의견과 그릇이 다르다 — 종속하는 값은 선택지에 붙고, 직교하는 값은 자기 문항을 갖는다.
_Avoid_: 문항 의견(직교하는 다른 것), 기타 입력(입력칸이라는 UI 어감)

**조사표 (survey document)**:
설문에 붙는 PDF 자료. 설문당 하나 이상이며 파일 참조는 **라이브**다 — 발행 뒤에도 교체할 수 있고 교체 가드를 두지 않는다. 테스트 파티션과 무관한 설문 단위 자산이라 테스트 모드도 같은 파일을 본다. (ADR 0020)
_Avoid_: 첨부파일(메일 첨부·공지 첨부와 다른 층), 설문지(응답 화면 자체와 혼동)

**영역 앵커 (region anchor, 줄여서 앵커)**:
조사표 쪽 위의 사각형. 질문 하나 또는 그룹 하나에 붙고 **한 대상에 여러 개**가 붙을 수 있다(한 블록이 3쪽과 4쪽에 걸치는 일이 흔하다). 좌표는 쪽 번호 + 정규화 0~1이며 화면 픽셀은 저장하지 않는다. 발행 시 버전 스냅샷에 복사돼 얼어붙는다 — 라이브면 분할 시작점이 진행 중인 응답의 발밑에서 움직인다. (ADR 0020)
_Avoid_: 하이라이트(표시 상태와 혼동), 영역 태그, 좌표 매핑

**분할 레이아웃 (split layout)**:
응답 페이지가 좌(조사표) / 우(질문)로 갈라진 상태. **설정이 아니라 파생이다** — 토글이 없고, **페이지마다** 판정한다. 앵커 걸린 항목을 담은 페이지만 갈라지고, 조사표에 등록되지 않은 페이지는 일반 문항 페이지로 나온다(안내문을 보여주고 다음을 누르면 조사표가 나온다). 판정은 스냅샷 구조 기준이라 조건부로 숨은 질문의 앵커도 세지만, 좌측에 사각형을 *그리는* 것은 표시되는 항목 것만이다. 레이아웃은 구조에서, 표시는 조건에서.
_Avoid_: 분할 모드(설정 어감), 2단 레이아웃(반응형 컬럼과 혼동), PDF 뷰어 모드

### 파일(R2) 수명주기

**고아 파일 (orphan file)**:
버킷 객체 중 전역 참조 유니온과 발송 장부 어디에도 걸리지 않는 것. 참조 유니온은 라이브 행·발행 스냅샷·보관함·캠페인 스냅샷을 포괄하며 URL/key 혼재 형태를 key 로 정규화해 비교한다. 공유 참조(복제·보관함·스냅샷이 같은 키 참조) 구조상 "특정 설문의 파일"이라는 소유 개념은 성립하지 않는다 — 고아 여부는 항상 전역 판정이다.
_Avoid_: 삭제된 설문의 파일, 미사용 이미지(스코프 불명)

**유예 삭제 (deferred deletion)**:
R2 영구 객체가 지워지는 유일한 경로. 삭제 후보(엔티티 삭제·저장 diff 에서 수집)를 등록하고 7일 유예 후 집행하며, 집행 직전 전역 참조를 재확인해 통과한 키만 지우고 결과를 검증·회계한다. 유예 중 참조가 부활하면 후보는 취소된다. 즉시 삭제는 존재하지 않는다. `tmp/` 는 이 관할 밖(R2 lifecycle 24h 소관)이다.
_Avoid_: 즉시 삭제, GC(일괄 청소 어감 — 과거분 백필은 하지 않기로 결정)

**발송 장부 (sent-keys ledger)**:
메일 발송(캠페인·테스트 공통) 시점에 발신 콘텐츠에서 추출한 R2 키의 append-only 기록. 장부에 오른 키는 참조 유무와 무관하게 영구 보존된다 — 수신함의 참조는 DB 로 복원할 수 없기 때문이다. 발송 여부의 소급 판정은 하지 않되, 장부 도입 시점에 실존하는 캠페인 스냅샷에서 1회 시딩한다(판정이 아닌 기록 이관 — 설문 삭제가 스냅샷을 연쇄 소멸시켜도 발송분이 보호되기 위함).
_Avoid_: 발송 이력 추정, 캠페인 스냅샷 스캔으로 발송 판정

**메일 템플릿 삭제 이원화**:
비테스트 캠페인 발송 시작 이력이 있는 템플릿은 soft delete(캠페인 이력 보전), 없는 템플릿은 hard delete. soft delete 된 행은 파일 참조 자격을 잃는다(발송분은 장부가 보호). 테스트 발송은 행 수명 판정에 영향을 주지 않는다.
_Avoid_: 템플릿 삭제 = 항상 soft delete(구 의미론)

### 분석 보고서

**조사 아키타입 (survey archetype)**:
설문의 큰 조사 목적. 실태조사, 만족도조사, 성과조사, 인식조사처럼 분석 추천의 기본 방향을 정하는 분류이며 개별 문항의 역할이 아니다.
_Avoid_: 조사 유형 태그를 문항 역할로 쓰기

**분석 변수 (analysis variable)**:
보고서 분석과 통계 검정의 최소 단위. 문항 하나가 아니라 SPSS export column 하나에 대응하며, 복수응답·테이블·순위형 문항은 여러 분석 변수로 펼쳐진다. 식별의 정본은 설문 구조 좌표(문항 + 셀/옵션/순위 같은 하위 단위)이고, SPSS 변수명은 좌표에서 파생되는 표시 라벨이다 — 자동 발번 변수명은 문항 편집 시 재발번되므로 영속 식별자가 될 수 없다.
_Avoid_: 문항 단위 통계 변수, SPSS 변수명을 영속 식별자로 사용

**문항 블록 (question block)**:
분석 워크벤치 화면의 표시·오버라이드 단위. question 엔티티 하나에 속한 분석 변수들의 묶음이며, questionCode 접두사 그룹이나 빌더의 `question_group`과 다르다. 후속 주관식처럼 연관된 다른 문항은 블록에 포함하지 않고 표시조건 관계 신호로 곁에 연결만 한다.
_Avoid_: 코드 접두사 묶음(`Q33*`), 질문 그룹과 혼용

**변수 프로파일 (variable profile)**:
분석 변수의 역할, 도메인, 척도, 값 타입, 응답 형태, 시간 성격, 분석 용도를 묶은 해석 메타데이터. 자동 추론 초안으로 만들되 사용자가 검토·수정할 수 있어야 한다.
_Avoid_: role 단일 enum, 문항 태그

**분석 레시피 (analysis recipe)**:
조사 아키타입과 변수 프로파일 조합에서 추천되는 분석 묶음. 차트, 기술통계, 교차분석, 집단 차이 확인, AI 초벌 해석의 기본 구성을 함께 정한다.
_Avoid_: 문항별 하드코딩 추천

**문항 요약 (question summary)**:
단일 문항 또는 문항 블록의 현황을 설명하는 분석. 빈도, 비율, 평균, 중앙값, Top2·Bottom2 비율처럼 보고서 본문에 바로 쓰는 기술통계를 포함한다.
_Avoid_: 통계 검정까지 포함한 요약

**집단 차이 분석 (group difference analysis)**:
segment 변수로 나눈 집단 사이에 outcome/evaluation/barrier 계열 변수가 다르게 나타나는지 확인하는 분석. 보고서 UI에서는 "차이 없음/차이 가능성/차이 뚜렷함/표본 부족"처럼 해석 중심으로 표현한다.
_Avoid_: 검정명 중심 메뉴

**AI 초벌 해석 (AI draft interpretation)**:
계산된 분석 결과와 주의사항을 근거로 작성되는 보고서 문장 초안. 원본 응답 JSONB를 직접 해석하지 않고 검증된 요약·교차표·집단 차이 결과를 입력으로 삼는다.
_Avoid_: AI 통계 판정, 원본 JSONB 자유 해석

**분석 설정 오버라이드 (analysis override)**:
자동 생성된 변수 프로파일·분석 레시피 위에 사용자가 분석 페이지나 리포트에서 덮어쓴 표시·지표·태그 설정. 원본 SPSS 메타데이터를 곧바로 바꾸는 것이 아니라 해당 분석 관점의 의도를 기록한다.
_Avoid_: 원본 태그 수정, 일회성 차트 상태

**표시조건 관계 신호 (display-condition relationship signal)**:
displayCondition의 sourceQuestionId와 requiredValues가 만들어내는 문항 간 의존 신호. 후속 문항의 분모, 필터 코호트, 추천 교차분석 후보를 정하는 강한 근거지만 인과관계 자체를 의미하지는 않는다.
_Avoid_: 분기 로직을 원인-결과 관계로 해석

### 워크스페이스와 권한

> 구현 상태: **앱 미착수, DB 선반영** (2026-08-19 확인 — 코드에 teams/workspace 스키마가 없다. 단 프로덕션 DB 에는 Better Auth 테이블 5종이 이미 존재하며 앱은 사용하지 않는다). 아래 어휘는 설계 합의(ADR 0006~0011)의 용어 정의이며 현재 코드의 설명이 아니다. 현행 권한 모델은 admin allowlist + 게스트 설문 grant다 (CLAUDE.md "인증과 권한").

**팀 (team)**:
정상 배치된 설문을 소유하는 최소 워크스페이스이자 기본 접근 경계. `연구1본부 - 1팀`처럼 전체 조직 경로를 이름에 포함하며 사용자는 복수 팀에 동시 소속될 수 있다.
_Avoid_: 별도 본부 엔티티, users.department, 조직명 문자열을 권한 판정에 사용

**시스템 전체 보기 (system workspace view)**:
슈퍼어드민이 모든 팀의 설문과 배치 대기 설문을 통합 조회·관리하는 가상 범위. 팀 엔티티가 아니므로 설문 소유, 멤버십, 설문 그룹, 해산, 재배치 목적지가 될 수 없으며 UI에서는 "메가리서치"로 표시한다.
_Avoid_: 메가리서치 팀, 기본 팀, 전체 팀

**팀 역할 (team role)**:
팀 멤버십에 붙는 역할 — 팀장(멤버 관리 가능)과 팀원. 팀에 게스트 역할은 없다(게스트는 설문 단위 개념).
_Avoid_: 팀 게스트

**슈퍼어드민 (superadmin)**:
팀 소속과 무관한 시스템 전역 관리자. 가입 승인, 팀 생성·배정을 전담하며 시스템 전체 보기와 모든 설문(private 포함)에 접근한다.
_Avoid_: 관리자(팀장과 모호), admin(기존 단일 admin 어감)

**승인 대기 (pending)**:
가입 직후 슈퍼어드민 승인 전의 사용자 상태. 세션을 발급하지 않으며 승인 시 active, 거절 시 rejected가 된다.
_Avoid_: 미인증(이메일 인증과 혼동)

**재직 중 (active)**:
정상 로그인할 수 있는 사용자 상태. 슈퍼어드민이 아닌 사용자가 소속 팀이 없으면 프로필 외 내부 설문은 초대 여부와 무관하게 보이지 않는다.
_Avoid_: 활성(화면 용어), 승인 완료

**일시 정지 (suspended)**:
로그인을 일시적으로 막되 팀 멤버십·설문 초대·소유권 기록은 유지하는 사용자 상태. 재직 중으로 복귀하면 기존 관계가 다시 적용된다.
_Avoid_: 퇴사, 비활성

**퇴사 (departed)**:
세션을 폐기하고 팀 멤버십과 설문 소유권을 정리해야 하는 사용자 상태. 과거 작성자·메일·감사 기록은 보존하며 일반 재활성화 대상이 아니다.
_Avoid_: 일시 정지, 계정 삭제

**재입사 처리 (rehire)**:
퇴사 사용자를 새 팀에 다시 배정하고 비밀번호 재설정을 거쳐 재직 중으로 전환하는 슈퍼어드민 작업. 이전 팀 멤버십과 설문 초대는 자동 복구하지 않는다.
_Avoid_: 재활성화, 퇴사 취소

**팀 미배치 사용자 (unassigned user)**:
재직 중이지만 active 팀 멤버십이 하나도 없는 일반 사용자. 승인 직후 팀 배정을 비웠거나 소속 팀이 해산되면 발생하며, 로그인과 프로필 접근만 가능하고 초대 설문을 포함한 모든 내부 설문 접근은 차단된다. 배치는 팀 상세의 팀원 추가 또는 재배치 센터의 팀 배정으로 한다.
_Avoid_: 게스트, 배치 대기 설문, 승인 대기

**팀원 추가 (team member pull)**:
팀 상세에서 팀장·슈퍼어드민이 미배치 사용자만 검색해 자기 팀으로 당겨오는 배치 경로. 추가 즉시 active 멤버십이 생겨 내부 설문 경로가 열린다. 타 팀 active 멤버 이동은 슈퍼어드민 전용이다.
_Avoid_: 타 팀 멤버 빼오기, 팀장에게 재배치 센터 접근 부여

**참여자 (collaborator)**:
설문 단위로 명시적으로 초대된 사용자. 편집자(editor)는 다른 사용자를 재초대할 수 있지만 열람자(viewer)는 재초대할 수 없으며, 어느 역할도 팀 멤버십을 만들지는 않는다.
_Avoid_: 공유 대상, 멤버(팀 멤버십과 혼동)

**게스트 (guest)**:
계정·이메일 없이 설문별 게스트 링크와 별도 비밀번호로 제한된 범위를 열람하는 외부 주체. 분석은 on/off, 현황은 표시 항목 화이트리스트로 허용한다. 다운로드(export)와 응답 상세·컨택 원본(연락처)·메일·편집은 항상 불가하다.
_Avoid_: 읽기 전용 계정, viewer 참여자, 게스트 사용자 역할

**현황 표시 항목 (overview display item)**:
게스트·viewer의 설문 현황 화면에 노출할 구성 요소의 화이트리스트(`viewerScopes.overviewComponents`) — 핵심 지표, 응답 추이, 상태별 분포, 문항별 이탈 구간, 진척률 리포트, 마스킹된 조사 대상 진척 리스트 등. 공유 설정에서 체크박스로 고르며 빈 목록은 현황 미노출을 뜻한다. 조사 대상은 마스킹본만 항목이 될 수 있고 연락처 원본은 항목이 될 수 없다. UI 표기는 「현황에서 보여줄 항목」.
_Avoid_: 현황 컴포넌트(개발 어휘 — UI 카피 금지), 차트 단위 분석 차단(분석은 on/off뿐)

**공개 범위 (survey visibility)**:
설문의 접근 규칙 — "팀 공개"(소속 팀 전원 + 참여자)와 "초대된 멤버만"(설문 소유자 + 참여자 + 슈퍼어드민, 팀장도 초대 없이는 불가).
_Avoid_: 공개 설문(응답 페이지의 isPublic과 별개 개념)

**capability**:
권한 검사의 최소 단위(설문 편집, 현황 열람, 다운로드 등). 역할은 capability 묶음 프리셋이며, 권한 분기는 역할명이 아니라 capability로 묻는다.
_Avoid_: 역할명 하드코딩 분기

**설문 그룹 (survey group)**:
팀 안에서 설문을 함께 정리하는 공용 폴더. 모든 active 팀원이 그룹 구조를 편집할 수 있지만, 설문 이동은 해당 설문의 편집 권한이 있는 사용자만 할 수 있다. 설문을 넣는 경로는 「설문 담기」(미분류 전용 일괄)와 카드 케밥 「그룹 이동」(단건, 미분류 선택 = 빼기) 둘이며 설문은 그룹 하나에만 속한다. 다른 팀으로 승계된 설문은 이전 그룹 관계를 끊고 새 팀의 미분류로 들어간다. 문항 편집기의 질문 그룹(`question_groups`)과는 전혀 다른 개념이다.
_Avoid_: 질문 그룹과 혼용, 폴더 권한(권한은 설문 단위 — 그룹은 시각적 묶음일 뿐)

**설문 소유자 (survey owner)**:
모든 설문의 발행·삭제·승계와 소유자 연동 연락처를 책임지는 현재 사용자. 최초 작성자 기록과 분리되며, 팀 전체 설문은 같은 팀의 active 멤버에게, 초대 전용 설문은 active 참여자에게 소유권을 이전할 수 있다.
_Avoid_: 방장, 최초 작성자, 설문 관리자

**소유자 연동 (owner-linked contact)**:
설문 문의·메일 회신 대상을 고정 주소가 아니라 현재 설문 소유자의 이메일로 해석하는 방식. 소유권 이전 후 발송되는 메일은 새 소유자에게 자동 연결되며 발신 주소와 과거 발송 기록은 바뀌지 않는다.
_Avoid_: 발신자 변경, 과거 캠페인 수정, 퇴사자 주소 복사

**승계 대기 (succession pending)**:
설문 소유자 퇴사 전에 후임자를 지정하지 못해 슈퍼어드민의 재배치가 필요한 상태. 승계가 끝날 때까지 일반 사용자의 소유권 변경은 중단된다. 팀 해산은 승계 대기를 만들지 않는다 — 해산 설문은 배치 대기로 수렴한다.
_Avoid_: 소유자 없음, 고아 설문, 자동 승계

**배치 대기 설문 (survey awaiting assignment)**:
기존 데이터 전환이나 팀 해산으로 아직 실제 소유 팀이 정해지지 않아 시스템 전체 보기에 보관된 설문. 슈퍼어드민만 내부 관리할 수 있으며 실제 팀을 지정하면 정상 배치 설문이 된다. 배치 대기 중에도 기존 공개 응답·게스트 열람·메일 발송은 계속된다.
_Avoid_: 기본 운영팀 설문, 메가리서치 팀 설문, 무소유 설문

**팀 해산 (team dissolution)**:
팀을 즉시 삭제하지 않고 보관(archived) 상태로 전환하는 수명주기. 경고 모달(영향 요약 + 팀 이름 입력)에서 확정하는 즉시 한 트랜잭션으로 팀은 archived, 팀원은 전원 미배치, 설문은 전부 배치 대기가 된다. 중간 상태와 해산 취소는 없으며 복구는 팀 재생성 후 재배치다. 전체 공유로 풀거나 설문 데이터를 삭제하지 않는다. (ADR-0011)
_Avoid_: 팀 삭제, 팀 데이터 삭제, 전체 공유 전환, 재배치 중(reassigning) 중간 상태

**재배치 센터 (reassignment center)**:
메가리서치(시스템 전체 보기) 카드로 진입하는 슈퍼어드민 전용 인박스. 미배치 사용자와 배치 대기 설문 두 탭으로 해산·전환 잔여물을 배치하며, 해산된 팀 이력은 지표로만 조회한다. 팀장은 접근할 수 없다.
_Avoid_: 팀장 공용 콘솔, teamId 딥링크 진입, 승계 대기 탭

### 응답 개인정보

**PII 문항 (pii question)**:
응답값을 암호화해 저장하도록 지정된 단답형·장문형 문항, 또는 표 질문 안에서 같은 지정을 받은 input 셀(이 경우 그 셀 값만 암호화되고 표의 다른 셀은 평문). 지정 여부는 응답의 버전 스냅샷과 현재 설정의 합집합 기준(어느 쪽이든 지정돼 있으면 암호화)이며, 관리자 화면(export·응답수정·분석)에서는 복호화된 평문으로 보인다. 조사 대상자(컨택) PII와 별개 개념 — 이쪽은 응답자가 설문 안에서 직접 입력한 값이다.
_Avoid_: 개인정보 질문(동의서 안내 문항과 혼동), 컨택 PII와 혼용

**개인정보 보관기한 (PII retention deadline)**:
설문 단위로 등록하는 날짜로, PII 문항 응답값을 보유하는 마지막 날이다. 해당 날짜를 포함해 그 KST 하루가 끝날 때까지 보유하고 이후 파기 대상이 된다(동의서의 "보유기간 ~YYYY.MM.DD." 해석과 일치). 버전 스냅샷 밖 — 실사 도중에도 연장·단축이 즉시 반영된다.
_Avoid_: 보유기간(문서 문구 그대로 — 기한 시점이 모호), 설문 종료일(endDate)과 혼용

**응답 PII 파기 (response PII destruction)**:
설문의 보관기한 경과 후 암호화된 응답값을 복구 불가능한 파기 마커로 치환하는 처리. 암호화된 값만 대상으로 하며(토글 이전 평문 잔존분은 대상 아님 — 의도된 한계), 재실행해도 안전한 멱등 처리다. 응답 행 자체를 지우는 soft delete와 다르고, 가리기만 하는 마스킹과도 다르다. (ADR-0012)
_Avoid_: 응답 삭제, 마스킹, PII 삭제(컨택 쪽 어휘)

### 쿼터

**쿼터 플랜 (quota config)**:
한 설문의 교차 셀별 목표 정의 전체 — 차원·카테고리·셀 목표·마감 문구·집행 스위치. `surveys.quotaConfig` JSONB 한 곳이 소유하며, 버전 스냅샷 밖이라 실사 도중 재배포 없이 편집된다. 카운트는 저장하지 않고 완료 응답에서 실시간 계산한다.
_Avoid_: survey_quotas 위성 테이블, 저장된 카운터

**쿼터 차원 (quota dimension)**:
쿼터를 나누는 축. 문항 1개에 바인딩하며 유형은 옵션형(보기값 집합)·숫자형(반열림 구간 min ≤ 값 < max). 성별·연령처럼 단일값 소스만 차원이 된다.
_Avoid_: 복수형 차원, 마진 합계 쿼터

**쿼터 셀 (quota cell)**:
차원들의 카테고리 조합과 목표 수. 셀 키는 categoryId를 차원 순서대로 이은 문자열(인덱스 아님 — 재정렬해도 목표 보존). "현재"는 완료 응답에서 실시간 집계하며 current ≥ target이면 "마감"이다.
_Avoid_: 인덱스 기반 셀 식별, 저장된 현재 수

**쿼터 마감 vs 쿼터마감 응답 (quota closed vs quotaful_out)**:
"마감"은 셀이 목표에 도달해 닫힌 상태(플랜 진척, 셀 수). `quotaful_out`은 마감된 셀에 걸려 종료된 응답의 처리 상태(사람 수)다. 서로 다른 층위이며 UI에서 섞지 않는다 — KPI 쿼터 카드는 진척(완료/목표·%·마감 셀 수)을, disposition은 별도로 표현한다.
_Avoid_: 마감 셀 수와 튕긴 응답 수 혼용

### 데이터 내보내기

**Raw 내보내기 모수 (raw export population)**:
Raw Data 엑셀(raw/raw-split)의 행 집합 — 삭제·테스트만 제외하고 전 상태(진행중·이탈 포함)를 포함하며 상태 컬럼으로 구분한다. 워크북의 모든 시트가 같은 행 집합을 봐서 순번이 정합해야 한다. SPSS .sav의 완료 전용 분석 모수와는 별개 개념이다. **옵션 — 조사 대상 기준 모수**: 다이얼로그의 「조사 대상 중 미응답자 포함」을 켜면 모수가 위 행 집합 ∪ 스코프 파티션에서 그 모수 응답이 하나도 없는 조사 대상이 된다(삭제 응답만 있는 대상도 미응답 — `contact_targets.response_id` 역참조가 아니라 raw 모수 술어 `NOT EXISTS` 로 판정). 미응답 행은 문항 열이 비고 상태가 「미응답」이며 정렬은 시스템ID 순(같은 시스템ID 안에서는 시작일시 순, 익명 응답은 뒤)이다. 기본은 응답 기준이고 설문 설정으로 저장되지 않으며, 조사 대상이 없는 설문(`hasContacts=false`)에서는 다이얼로그에 Raw Data 옵션 영역 자체가 없다(명단 열 토글도 같은 영역). 한도 가드(`MAX_EXPORT_RESPONSES`)는 응답 수 + 미응답 조사 대상 수 합으로 판정한다. 로더는 `lib/analytics/raw-export-rows.server.ts`(`loadRawExportRows`·`countRawExportPopulation`), 순수 조각(미응답 행·정렬·명단 값 조립)은 `raw-export-rows.ts`.
_Avoid_: 분석 모수와 혼용, 완료 응답 내보내기, 조사 대상 명단 열과 혼동(그쪽은 열, 이쪽은 행)

**응답 메타 컬럼 (response meta columns)**:
Raw Data 시트와 분할 시트 왼쪽에 붙는 운영 메타 열 묶음 — IP 해시·번호(systemID)(=resid)·순번·조사 대상 그룹·개별 URL·상태·마지막 입력 문항·시작일시·종료일시·소요시간·접속 단말 순. 상태 열은 `formatExportStatusLabel` 어휘(자격 미달은 「완료(자격 미달)」)이고, 조사 대상 기준 모수의 미응답 행에는 「미응답」이 나온다 — `NOT_RESPONDED_STATUS`(`not_responded`)는 조사 대상 기준 모수 전용 값으로 DB 에 없고 콘솔 pill 도 받지 않으며, 그 행의 번호(systemID)·조사 대상 그룹·개별 URL 은 조사 대상 값이고 IP 해시·마지막 입력 문항·시작일시·종료일시·소요시간·접속 단말·브라우저는 빈칸이다(그룹 없는 대상의 그룹 열도 빈칸 — 「공개링크」·「—」 폴백은 응답 행 전용). 번호(systemID)와 조사 대상 그룹은 설문 설정 기준 조건부 열이다 — 컨택 타겟이 없는 설문은 번호 열을, 그룹값이 전무한 설문은 그룹 열을 만들지 않는다(응답 매칭 여부 무관). 순번은 항상 존재한다. SPSS 변수가 아니므로 코딩북·.sav에는 포함하지 않으며 헤더는 3행 세로 병합이다. 같은 resid 열이 운영 콘솔 응답 목록에서는 「번호(ID)」로 표기된다 — 값·조건부 생성 규칙은 동일하고 표기만 화면별로 다르다(엑셀=번호(systemID), 콘솔=번호(ID)).
_Avoid_: 식별자 모드(systemID/순번 택일 — 폐기된 개념), 메타 변수, 빈 값 기준 열 생략(기준은 설문 설정), 두 표기를 다른 데이터로 오해

**조사 대상 명단 열 (contact roster columns, 옵션)**:
Raw Data·분할 시트에서 응답 메타 열 바로 오른쪽, 문항 열 왼쪽에 붙는 컬럼 스킴의 `attrs.*`·`pii.*` 열 묶음. 다이얼로그의 「조사 대상 명단 열 포함」 토글로만 켜며(기본 꺼짐, 설문 설정에 저장되지 않는다) 숨김 열도 포함하고 `system.*` 은 제외한다 — 시스템ID·그룹은 메타 열이 이미 갖고 있다. 순서는 스킴 order, 헤더는 메타 열과 같은 3행 세로 병합, 라벨은 스킴 label(비면 key). SPSS 변수가 아니라 코딩북·`.sav`·`.sps` 에 없고 응답 내역 시트에도 붙지 않는다. PII 열은 조사 대상 엑셀과 같은 복호화 경로(`decryptPiiForExport`)로 평문이 나가며, pii 열이 하나라도 있으면 응답에 `Cache-Control: no-store` 가 붙는다. 익명 응답 행은 공백이고, 「조사 대상 중 미응답자 포함」과 독립이라 둘 다 켜면 미응답 행에도 명단 값이 찬다. 열 선택은 `lib/operations/contacts.ts` `selectRawExportContactColumns`(순수), 값 조회는 로더가 컨택 조회에 `attrs` 를 얹고 복호화를 1회만 부른다(꺼지면 스킴·attrs·복호화 조회가 일어나지 않는다).
_Avoid_: 이월 요약과 혼동(그건 값의 의미이고 이건 열 묶음), 응답 메타 열의 일부로 세기, 코딩북 변수, 컬럼별 내보내기 플래그(없다 — 토글은 전부를 붙인다)

**IP 해시 (ip hash)**:
원본 IP 대신 저장되는 단방향 해시(비밀 salt 포함, sha256 64자). 원본 IP는 어디에도 저장하지 않으며 복호화·역조회는 비목표다. export에는 앞 16자만 표기하고 "같은 값 = 같은 IP" 식별(중복 검수) 전용이다.
_Avoid_: IP 주소, IP 복원

**개별 URL (short invite URL)**:
컨택 매칭 응답의 단축 초대 URL(`/i/{inviteCode}`). inviteToken(UUID) 장형이 아니라 inviteCode 기반 단축형이 정본이며, 익명 응답은 공백이다.
_Avoid_: invite 토큰 URL(?invite=uuid 장형)

**마지막 입력 문항 (last answered question)**:
응답값이 실제 존재하는 질문 중 최후순 질문의 질문코드(questionCode, 예: Q6) — 이름 그대로 입력 기준이며, 한 페이지에 여러 문항이 있어도 실제 입력한 마지막 문항을 가리킨다. 응답이 전혀 없으면 진행 위치(currentStepId) 페이지 라벨(도달 위치)로 폴백하고, 그래도 없으면 공백이다. 라벨은 질문코드 우선·제목 Qx 파싱 폴백이며 위치 서수("N번째")는 쓰지 않는다. 셀 단위 정밀도는 아니다(셀 단위 추적은 별도 응답 페이지 확장 필요). 운영 콘솔 진척 표기(현재 페이지 위치)와는 기준이 다르다 — 이쪽은 "어디까지 입력했나", 그쪽은 "지금 어디 있나".
_Avoid_: 진행 위치와 혼용, N번째 위치 표기

**내보내기 셀 순서 (table export cell order)**:
테이블 문항 변수 열의 나열 축 — 행 우선(기본: 행 고정 → 열 순회)과 열 우선(열 고정 → 행 순회). 문항별 설정이며 Raw·분할·코딩북·.sav 전 소비자에 동일 순서로 적용된다. 변수명·응답값은 불변이고 나열 순서만 바뀐다. 버전 스냅샷 밖 — export는 현재 설정 기준이다.
_Avoid_: x축 y축 전환(데이터 전치 어감), 열 정렬

---

## 추적조사

**이월 요약 (prior summary)**:
지난 회차 조사가 그 사람에 대해 내린 결론 몇 가지 — 조사 방법, 그때의 상태, 소속 기업, 비고. 조사 대상 명단에 딸려 들어오는 값이며 조사 대상 속성(attrs)의 일부다. 지난 회차에 그 사람이 문항에 답했는지와 무관하게 존재한다 — 응답을 받지 못해 2차 자료로 조사해 채운 대상에게도 있다. 문항 본문에 값을 끼워 넣거나 표시 조건의 피연산자로 쓰는 것은 이쪽이다.
_Avoid_: 작년 응답(응답이 아니다), 이월 응답과 혼용

**이월 응답 (prior answers)**:
조사 대상 한 명이 지난 회차에 문항에 답한 내용 한 벌 — 현재 회차 응답과 같은 모양(질문 단위 값 묶음)이며, 표·복수선택·랭킹이 그대로 들어간다. 개별 링크로 들어온 응답자의 화면에 채워져 보이는 값의 출처이고, 변동 확인이 붙는 기준이다. 회차가 넘어가도 덮어쓰지 않고 새 회차 응답을 따로 쌓는다 — 전년대비 증감을 낼 수 있어야 하기 때문이다. 지난 회차에 실제로 답한 대상에게만 있다는 점에서 이월 요약과 다르다.
_Avoid_: 이전 데이터(무엇의 데이터인지 불명), 작년 attrs, 프리필 값(프리필은 이월 응답을 쓰는 한 가지 방식일 뿐)

**이월 값의 자리 — 표시와 응답은 다르다**:
이월 응답은 응답값(`responses`)에 미리 깔리지 않는다. 이월 값이 있는 문항은 잠긴 채 그 값을 **보여주기만** 하고, 응답자가 변동 확인을 밝히는 순간 문항 단위로 이번 회차 응답에 복사된다. "같음"은 잠긴 채로 지나가고 "달라짐"은 값이 채워진 채 입력이 열린다. 이 구분이 "응답자가 보지 못한 문항은 빈칸으로 나간다"를 성립시킨다 — 아무도 확인하지 않은 지난 회차 값이 올해 통계에 섞이지 않는다.
_Avoid_: 프리필(값이 이미 이번 회차 응답이라는 어감), 자동 입력

**이월 값과 본문 프리필 토큰의 우선순위**:
단답형·표 input 셀에 걸린 본문 프리필 템플릿(`defaultValueTemplate`)은 조사 대상 속성 채널이다 — 이월 요약을 표시하는 읽기 전용 칸이지 응답자가 답하는 칸이 아니다. 그래서 템플릿이 걸린 문항은 이월 응답이 있어도 템플릿 값이 이긴다. 화면은 입력을 잠그고 서버는 완료 처리에서 기대값으로 강제 복원한다 — 두 경계가 같은 규칙이다. 이월 응답으로 채우려면 그 문항에서 템플릿을 빼야 한다.
_Avoid_: 이월 값이 우선한다는 가정, 문항별로 다르게 동작한다는 가정

**변동 확인 (change confirmation)**:
이월 응답이 있는 문항에서 응답자가 "작년과 달라졌는가"를 밝히는 행위와 그 기록. 문항 단위로 남으며, 밝히지 않으면 다음으로 넘어갈 수 없다. 이 장치가 없으면 아무도 보지 않은 지난 회차 값이 이번 회차 응답으로 그대로 흘러나간다 — 변동 확인은 그 세탁을 막는 근거다. 응답 필수 여부(required)와는 별개 축이다.
_Avoid_: 검증(validation — 값의 형식·범위를 보는 기존 개념), 수정 여부

**출력 병합 (export merge)** — 하지 않기로 한 것:
"내보내기 시 이월 응답 위에 이번 회차 응답을 겹쳐 한 장으로 만든다"는 발상이었으나 **구현하지 않는다**. 변동 확인이 "같음"에서 이월 값을 이번 회차 응답으로 복사하므로, 완료된 응답은 저장 시점에 이미 "지난 회차 값 + 올해 수정분"이 합쳐진 완전한 한 벌이다. 응답자가 보지 못한 문항이 빈칸으로 나가는 것도 같은 이유다 — 채울 자리가 애초에 없다.
_Avoid_: 내보내기가 두 회차를 합친다는 설명, 덮어쓰기, 업데이트

**변동 확인 변수 (change confirmation variable)**:
문항별 변동 확인 결과를 담는 내보내기 전용 파생 변수 — 이름은 문항 변수명 + `_CHG`, 값은 `1=지난 회차와 같음` / `2=달라짐`, 밝히지 않았거나 도달하지 못한 문항은 결측이다. 응답 **값**만으로는 낼 수 없는 지표라 따로 나간다: "같음"이 지난 회차 값을 그대로 복사하므로 값만 보면 확인한 사람과 확인하지 않은 사람이 구분되지 않는다.
이 변수가 생기는 문항 집합은 **이월 응답의 문항 키**에서 온다(응답에 남은 확인 기록이 아니다 — 그쪽을 보면 필터·진행 상황에 따라 같은 설문의 두 내보내기가 서로 다른 변수 집합을 낸다). 그래서 내보내기 경로가 이월 응답을 한 번 조회한다 — 값은 읽지 않고 키만 본다.
_Avoid_: 변동 플래그, 수정 여부 변수
