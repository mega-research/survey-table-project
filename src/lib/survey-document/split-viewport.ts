/**
 * 분할 레이아웃이 쓸 만한 최소 뷰포트 폭(px).
 *
 * 이보다 좁으면 좌우 각 판이 500px 도 되지 않아 조사표 한 쪽이 읽히지 않는다.
 *
 * 판정을 **폭**으로 하는 이유는 하나다 — User-Agent 판정은 태블릿을 오판하고
 * 데스크톱의 좁은 창을 못 잡는다. 그래서 기각했다.
 *
 * 상수를 따로 두는 것은 응답 화면 게이트와 문서·테스트가 같은 숫자를 보게 하기
 * 위해서다. 이 값을 바꾸면 안내 화면이 뜨는 경계가 그대로 움직인다.
 */
export const SPLIT_MIN_VIEWPORT_WIDTH = 1024;
